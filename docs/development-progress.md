# AI 大逃杀开发进度

最后更新：2026-09-02

## 当前结论

比赛是实时 Convex 模拟。`DecisionDriver` 通过观众租约成为唯一的调度器，按 12 秒节奏请求 Convex Action；Action 使用云变量 `DEEPSEEK_API_KEY` 调用 OpenAI 兼容的 DeepSeek 接口，再提交结构化行动。浏览器、Convex 数据库、行动日志和公屏都不会接收或保存密钥。

`battleRoyale.ts` 会校验模型动作的存活、冷却、区域邻接、开放禁区、同区目标、射程与物资。模型超时、CORS/网络失败、非法 JSON、无效动作和 240 次额度耗尽，都会产生审计事件并回退到规则 AI。

参考仓库每张配表的抽象位置、运行状态与未消费字段见 [参考配表覆盖矩阵](./reference-table-coverage.md)。

## 已完成

| 模块 | 状态 | 实现位置 |
| --- | --- | --- |
| 禁区预警与收缩倒计时 | 完成基础版 | `battle.zoneClosesAt`；中文预警、战略总览倒计时和 DeepSeek 上下文共享同一时间点 |
| 12 名角色、区域、关系、物品池 | 完成 | `data/battleRoyaleConfig.ts` |
| 12 名可执行角色卡 | 完成 | 每人独立目标、战斗/说话方式、攻击/结盟/撤退权重和专属战斗、结盟、交易台词；规则 AI 与 DeepSeek 共用配置 |
| 同区相遇决策 | 完成基础版 | 修复跨区角色被误选为最近敌人的问题；同区近距离相遇按人设、关系、双方生命和固定 seed 决定攻击、结盟、撤退或观察 |
| 强制近距交互与关系变化 | 完成 | 两名 AI 进入 2.2 格后绕过 12 秒模型节奏立即射击或交谈；同伴交流、停火、试探、警告分别产生正负关系变化，18 秒配对冷却抑制重复事件 |
| 对话导播与视野过滤 | 完成 | 双人交谈进入自动导播热点；手动锁镜时仅在发言者距焦点 8 格内或位于焦点区域时显示对话框，单人应援回应不触发交谈切镜 |
| 强化弹道与命中特效 | 完成 | 远程武器显示枪口闪光、白芯橙边曳光、移动弹头和双层冲击环；近战只显示冲击效果，事件保存武器类型 |
| 角色应援与阵营经营 | 完成基础版 | 浏览器本地保存应援角色、阵营声望和单局任务领取；阵营空投使用服务端 `FAN_01`，消耗真实干预点并产生角色状态、对白、地图效果和直播事件 |
| 首次介绍与加载状态 | 完成 | 首访三幕中文背景介绍；Convex 连接、引擎唤醒和 12 人状态同步均有可见加载界面；预留 `VITE_INTRO_VIDEO_URL` |
| 战斗、搜索、购买、结盟、治疗、禁区 | 完成，模型/规则统一执行 | `convex/aiTown/battleRoyale.ts` |
| 持续寻路与可见交谈 | 完成 | 空闲角色持续沿区域导航点 A* 巡查；模型/规则结盟及交易记录双方中文台词，直播气泡、公屏和角色抽屉同步展示 |
| 游戏对话框 | 完成 | 场内角色头顶只显示交谈状态符号；完整结盟/交易台词按发言顺序显示在直播画面下方的双角色对话框，并保留头像和姓名 |
| 寻路停滞恢复 | 完成 | 依据真实坐标进展检测碰撞等待和失效路径；等待 1.8 秒或移动 2.8 秒无进展时自动释放旧路径、避开拥挤目标并重新规划 |
| 真实移动状态与局部巡逻 | 完成 | `MOVE` 仅在服务端速度大于 0 时展示；规划和避让使用独立状态。日常巡逻优先选择半径 6 格内的可行走目标；战斗角色不再受已被新战场替换的 AI Town 旧墙体和已淘汰角色阻挡 |
| 跑团式区域剧情与直播降噪 | 完成基础版 | 24 条区域剧情具备场景、选择、D20 属性检定与成功/失败分支；直播仅弹出关键事件，剧情进入独立可收起卡片 |
| DeepSeek 剧情主动选择 | 完成 | `investigate` 必须提交当前区域的 `storyEventId` 与事件专属处置；服务端校验区域、触发次数、消费状态和剧情道具，选择写入检定、热度/关系后果、决策审计与行动日志，非法选择不计额度 |
| 云端 DeepSeek 战术决策 | 完成，待管理员配置密钥 | `DecisionDriver.tsx` + `cloudDecision.ts` + `submitAIDecision()` |
| 驾驶权、超时回退与审计 | 完成 | `battleState` / `BattleStats` 决策字段 |
| 逻辑区域图、导航栅格、地标碰撞、邻接迁移与红区伤害 | 完成 | `data/battleArena.ts` 生成 80×60 权威栅格；13 区、17 条邻接走廊、出生点与 26 个地标障碍共同驱动服务端 A*，Pixi 可显示焦点区碰撞占地 |
| 区域资源与物品即时效果 | 完成基础版 | `areaResources` + `ITEM_EFFECTS`；食物/饮料可恢复饱食或压力并影响后续 AI 决策 |
| 热度、连击、主线和隐藏任务 | 完成 | `battleRoyale.ts` |
| 干预点与 18 个干预操作 | 完成 | `INTERVENTION_OPERATIONS`，含阵营应援空投 |
| 双角色悬赏追杀 | 完成 | `RUL_04` 保存执行者与目标；规则 AI 优先跨区追踪，完成、第三方截杀或执行者淘汰均有独立结算播报 |
| 扫雷结算干预点 | 完成 | `BattleRoyalePanel.tsx` |
| 24 条区域剧情、3 条全局事件与运行时触发 | 完成基础版 | `AREA_SPECIAL_EVENTS` + `GLOBAL_SPECIAL_EVENTS`；中文播报含实际效果摘要和触发次数 |
| 参考剧情触发条件精确化 | 完成基础版 | 校正校园广播磁带、暗巷交易 60 秒、药品过期 30% 搜索概率、远程武器殉爆、单次搜索事件；角色专属剧情物仅进入所属角色掉落池 |
| 剧情专属主办方干预 | 完成基础版 | `STO_01` 至 `STO_06` + `applyIntervention()` |
| 剧情干预状态测试 | 完成 | `convex/aiTown/battleRoyale.test.ts` |
| C12 身份卡、线索和真相之间 | 完成 | `battleRoyale.ts` |
| 地图干预/剧情落点特效 | 完成 | `BattleRoyalePanel.tsx` + `index.css` |
| 剧情封锁与角色行动审计 | 完成基础版 | 格斗笼封锁会拒绝移动；总览和角色抽屉显示倒计时、区域规则与最近结构化行动 |
| CloudBase 前端、Convex 后端 | 已部署 | 见 README |
| 直播跟随、自动导播、角色详情抽屉 | 完成基础版 | `Game.tsx` + `LiveBattleHud.tsx` + `BattleCharacterDrawer.tsx` |
| 种子 RNG、行动日志和回放检查点 | 已完成动作补丁版 | `battleRoyale.ts` + `src/lib/battleReplay.ts`；每 30 秒保存检查点，模型、规则、世界周期、观众得分和主办方干预只记录变化字段，回放按 `actionId` 顺序还原且停止模型调度 |
| 原创 13 区战场底图、12 人头像与场内精灵图集 | 完成基础版 | `public/assets/battle/`；头像用于 HUD/档案，全身俯视角精灵用于 Pixi 战场，并具备步态起伏、朝向翻转、脚底跟拍环、生命条和淘汰态 |
| 13 区独立可交互直播地标 | 完成基础版 | 堡垒、演播塔、书库、格斗笼、医院等拥有独立透明图标；Pixi 锚点可直接点选并平滑切镜，底部区域卡展示危险、资源、开放/封锁、在场人数、机制和 Buff |
| 参考仓库美术资源接入 | 第一批完成 | 直播与总览共用参考纯地图；事件图标、生命电池和按钮态已接入；区域锚点已按新地图重新校准 |

## 未完成与风险

| 优先级 | 项目 | 说明 |
| --- | --- | --- |
| P1 | 区域剧情精确分支 | 24 条剧情已有场景、检定和成败分支，每条均生成三种带事件名称和行动文本的专属处置；DeepSeek 明确选择事件，校园广播磁带与监控权限卡会校验并消费。区域主人获得可审计的熟悉地形 +1，专属剧情物不会掉给其他角色。 |
| P1 | 关系网深度效果 | 关系强度、结盟、攻击背叛、交易、挑拨、重逢、守护、逆转和隐藏关系揭露已持久化；更细的人设分支仍待补齐。 |
| P2 | 应援长期经营 | 当前声望和任务领取保存在单个浏览器；跨设备账号、赛季阵营榜、阵营共同目标与反向应援事件仍需身份系统后再接入。 |
| P2 | 稳定回放 | 已完成。种子 RNG、唯一行动 ID、位置历史、持久化检查点与动作状态补丁共同还原检查点后的模型/规则行动、世界周期、干预和小游戏结算；HUD、区域层和角色抽屉均读取回放时刻，不调用 DeepSeek。 |
| P2 | 区域关卡 | 已完成确定性逐格导航层：新开局将 AI 放到所属区域的可走出生点；区域多边形、17 条邻接走廊、26 个地标障碍、导航节点、禁区和 13 套可点击地标图标均使用同一份关卡数据。剩余是把矩形地标占地升级为贴合美术的精细轮廓。 |
| P2 | 运动与社交表现 | 战术决策和高频移动已解耦；交谈会短暂停步，结束后恢复巡查。`dialogueLog` 最多保存 40 条清洗后的中文台词，不保存提示词、密钥或模型原始响应。 |
| P2 | 战场视觉反馈 | 永久禁区与剧情封锁在 Pixi 战场显示动态红色覆盖、警戒环、封锁标记和倒计时；直播 HUD 按顶部控制、左侧态势、右侧剧情、底部角色分区。 |
| P2 | 直播导播语言 | 自动导播优先追踪 6.5 秒内的战斗、淘汰、剧情、联盟和干预事件；平静期每 8 秒巡场，切镜使用逐帧阻尼平移并显示切镜原因与 LIVE 状态。 |
| P2 | 战斗与直播降噪 | 陌生/负关系遭遇提高攻击权重，可信关系仍可结盟，低血量仍会撤退；近距交互冷却延长至 36 秒，战斗喊话冷却为 18 秒，现场交谈展示缩短并限制同时可见气泡。非焦点地标收起名称，减少镜头遮挡。 |
| P2 | 片头与声音系统 | 用户提供的 6.58 秒视频已转为网页优化版片头；进入直播包含扫描、闪白和转场音。CC0 背景音乐与枪击、命中、淘汰、禁区、胜利、UI 音效按事件播放，声音偏好保存在浏览器。 |
| P2 | 平滑镜头与战场分层 | 镜头与角色共用历史位置插值，并由 Pixi 每帧执行帧率无关阻尼跟随；战场原画是不透明视觉底层，碰撞权威已切换到区域导航栅格。回放面板仅在回放时显示，剧情展开卡 7.5 秒后自动收起。 |
| P2 | 物品与禁区 | 稀有度、交易价值、加权掉落、医院/格斗笼/市场 buff、资源余量和持续红区伤害已完成基础版；权限卡监控剧情已具备前置、消耗和中文审计。 |

参考素材复核（2026-09-01）：已逐文件检查 `HanataniTakahiro/AIdataosha`。参考仓库的 `纯地图.png` 与当前 `arena-live-map.png` 校验值一致，没有另一张更精细的实时战场地图。仍未接入的资源主要是任务卡、日志弹窗、标签/按钮多状态、阈值条与容器切片；仓库没有 12 名角色场内精灵，本项目已基于现有原创头像身份生产并接入俯视角全身图集。

## 当前开发里程碑：P2 观赛与内容深度

1. 完成直播跟随、战略总览、角色抽屉、单一重置入口和回放控制的 UI 验收。
2. 继续把已上线的 24×3 事件专属处置扩展为更多人物特有后果；事件 ID、选项、道具前置与消费现已进入模型和服务端协议。
3. 将 26 个通用地标矩形碰撞升级成逐地标精细轮廓，并补充资源点、剧情点和封锁机关的场内交互。
4. 扩展长局回放压力测试和旧局兼容夹具；固定 seed、行动排序、规则执行与检查点后补丁还原已进入自动测试。

## 验证清单

- `npm run build` 通过。
- `npm test` 通过：123 项测试，包含悬赏任务双角色语义、跨区追踪、执行者奖励、第三方截杀失效，以及陌生角色攻击倾向覆盖。
- 浏览器新局验收通过：单次确认恢复 12 名角色、15/30 干预点、240 次模型额度、区域资源与剧情；自动导播可切到正在移动的训练场角色，焦点区地标碰撞占地可见。
- 桌面截图验收：场内角色显示为可辨认的全身精灵，镜头锁定环、生命条、KO 与移动状态不遮挡直播 HUD。
- Convex 新局创建 12 名唯一角色。
- 扫雷分数写入 `interventionPoints`，上限为 30。
- 主办方干预写入事件流并产生地图效果状态。
- 角色剧情、区域剧情和 C12 真相线均写入 `world.battle.feed`。
- 人工验证：同区/相邻移动会执行，非邻接 LLM 移动会被拒绝并记录中文原因。
- 云端密钥验收：管理员使用 `npx convex env set DEEPSEEK_API_KEY --deployment dev` 设置密钥后，检查至少一条 `source: model` 行动和一条模型失败后的规则回退；密钥不应出现在浏览器网络请求和 Convex 状态查询中。
